function SearchBar() {
  return (
    <input
      type="text"
      placeholder="Search..."
      style={{
        width: "300px",
        padding: "10px",
        borderRadius: "5px",
        border: "1px solid #ccc",
        marginBottom: "20px",
      }}
    />
  );
}

export default SearchBar;